# PostgreSQL Client Authentication Configuration
# FinBot v4 - Master and Replica Access Control

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust

# IPv4 local connections:
host    all             all             127.0.0.1/32            md5
host    all             all             0.0.0.0/0               md5

# IPv6 local connections:
host    all             all             ::1/128                 md5

# Replication connections
host    replication     replicator      0.0.0.0/0               md5
host    replication     replicator      ::0/0                   md5

# Docker network connections
host    all             all             172.16.0.0/12           md5
host    all             all             192.168.0.0/16          md5
host    all             all             10.0.0.0/8              md5

# Application connections
host    finbot_v4       postgres        0.0.0.0/0               md5
host    finbot_v4       finbot_app      0.0.0.0/0               md5
host    finbot_v4       finbot_readonly 0.0.0.0/0               md5

# Monitoring connections
host    all             postgres        172.16.0.0/12           md5